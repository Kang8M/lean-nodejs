<%- include('../header', {user: null, pageTitle: null}) %>

<h1>Users</h1>
<p>Click a user below to view their pets.</p>
<ul>
  <% users.forEach(function(user){ %>
    <li><a href="/users/<%= user.id %>"><%= user.name %></a></li>
  <% }); %>
</ul>

<%- include('../footer') %>